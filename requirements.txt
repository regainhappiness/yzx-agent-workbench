# ============================================================================
# Agent-demo 依赖清单
# 安装: pip install -r requirements.txt
# ============================================================================

# 核心框架
langchain
langchain-core
langgraph

# 模型 Provider (按需安装)
langchain-openai
langchain-deepseek
#langchain-anthropic

# 环境与工具
python-dotenv>=1.0.0
pydantic>=2.0.0
tenacity>=8.0.0
cryptography>=43.0.0

# ============================================================================
# FastAPI 服务层
# ============================================================================
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.20
aiofiles==25.1.0
sse-starlette>=2.0.0

# ============================================================================
# 测试
# ============================================================================
pytest

pytest-asyncio>=0.24.0
httpx>=0.27.0

# ============================================================================
# 文档处理
pypdf>=5.0
python-magic>=0.4
aiosqlite>=0.20.0

# ChatAgent SQLite 检查点 + 长期记忆持久化
# 3.0.1 修复 SQLite checkpointer 元数据过滤的 SQL 注入问题
langgraph-checkpoint-sqlite>=3.0.1

# MCP 工具接入 (外部 MCP Server 客户端)
mcp>=2.0

# 阿里云 OSS (可选)
alibabacloud-oss-v2

# Milvus 向量数据库
pymilvus>=2.4.0

tavily-python==0.7.25

# 可选: PostgreSQL 持久化存储 (用于 production)
# ============================================================================
# pip install langgraph-checkpoint-postgres langgraph-store-postgres psycopg[binary]
#
# langgraph-checkpoint-postgres  — PostgresSaver (短期记忆持久化)
# langgraph-store-postgres       — PostgresStore (长期记忆持久化)
# psycopg[binary]                — PostgreSQL 驱动
